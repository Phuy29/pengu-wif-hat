import { Button } from "./ui/button";

export function ConnectButton() {
    return (
        <>
            <Button onClick={
                () => { }
            }>Connect wallet</Button>
        </>
    );
}